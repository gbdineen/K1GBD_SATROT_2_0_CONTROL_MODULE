#include <ArduinoJson.h>

class JSON_Server
{
    private:
        

    public:
        JSON_Server(/* args */);
        ~JSON_Server();
};

JSON_Server::JSON_Server(/* args */)
{
}

JSON_Server::~JSON_Server()
{
}
